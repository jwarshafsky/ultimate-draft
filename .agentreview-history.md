# Agent Review History — Ultimate Draft

Iterative multi-agent reviews. Each round varies the specialist mix; this file is the "do NOT re-report" input for future rounds.

## Round 1 — 2026-06-27 — focus: Mock Draft feature

**Agents:** fantasy-auction domain expert, simulation-correctness engineer, interactive-UX designer, frontend engineer, QA/edge-cases

**Shipped this round:**
- [shipped] Interactive `_completeSale` now voids a sale when the winner has no slot for the player (parity with headless engine) — was charging budget + burning a slot + polluting slotsByPos[null] — QA/frontend
- [shipped] Keeper slot/eligibility looked up by NORMALIZED name in `buildMockTeamStates` (`_mockPlayerValue`) — accented keepers were falling to UTIL and corrupting open-slot/needs model — QA/sim
- [shipped] Stale-timer generation guard (`_interactive.gen` + `_later`) — a stopped/restarted/backgrounded interactive mock no longer keeps mutating state when old setTimeouts fire — frontend
- [shipped] Bid buttons are now relative (+$1/+$2/+$5) resolved against LIVE currentBid at click + a "→ $par" quick-bid + userBid auto-bumps to currentBid+1 — valid clicks were being rejected after an AI bump — frontend/UX
- [shipped] Buy/Fair/Overpay verdict chip (wired the unused `.otc-signal` CSS) + "Your max bid" shown in the bidding panel — price-discipline signal at the decision moment — UX
- [shipped] `inflationForMockState` uses the TRUE stored `keptCost` (not back-computed from a floored budget) — fixed inflation for over-keepered teams — sim
- [shipped] Mild, budget-conserving TIERED (star-concentrated) inflation — keeper-league realism; weights normalized over rosterable so total spend is unchanged (Monte Carlo: ~99% of budget, stars ~1.05-1.2x, scrubs under) — auction
- [shipped] `userNominate` matches by normalized name — frontend
- [shipped] Honest "Pool Exhausted (N slots open)" done-state instead of a false "All rosters filled" — QA
- [shipped] Monte Carlo percentiles use linear interpolation (old floor() indexing skewed p10/p90 and mislabeled median for tiny samples) — sim

**Deferred (with reason):**
- [deferred] Positional RUNS in nomination + drive nomination from `profile.nomMix` — `chooseNomination` ignores posScarcity/nomMix; needs a nomination-strategy redesign — auction
- [deferred] Endgame "scramble" realism (cash hoarders, $1 stars falling, teams genuinely stuck) — conflicts with the stability floors (safeBudget, $1/slot reserve); needs careful tuning — auction
- [deferred] Unify interactive vs headless bidding model (interactive uses a permanent `passedTeams` set; headless re-evaluates each round) so their price distributions match — sim
- [deferred] Staggered AI-bid pacing + real SOLD banner + bid log + a draft-day countdown timer — live-auction feel / commercial parity — UX
- [deferred] Board search + "needs only" filter + tier-cliff rules — UX
- [deferred] Live category/standings projection ("you're light on power/saves") during the draft — auction/UX
- [deferred] `computePosScarcity` supply ignores multi-eligibility (counts by single posKey vs demand summed across flex slots) — sim
- [deferred] Custom-bid input loses focus across the AI-bump re-render — frontend

**The ONE thing this round:** Make clearing prices realistic — a budget-conserving market price (value × inflation × positional scarcity) with mild star concentration — so the sim trains real price discipline instead of systematically overpricing. (Done.)

**What this round chose NOT to cover (for future rounds):** the Auto-mode result/Monte-Carlo/backtest result VIEWS' UX; AI nomination-strategy depth; draft-day timer/pacing; category-standings projection; accessibility; performance of large-N Monte Carlo.

## Round 2 — 2026-06-27 — focus: implement ALL of Round 1's deferred items + a polish pass

Not a fresh agent panel — this round implemented every deferred item from Round 1 ("implement everything, then take another pass and make it even better").

**Shipped (engine — mock-engine.js):**
- [shipped] nomMix-driven nominations (`_weightedPick` over target/drain/dump/blocker) + positional-run awareness via `inflation.posScarcity` hot positions; loyalty-target early, burn budget in endgame — was Round 1 deferred "positional runs"
- [shipped] endgame scramble: cashMult (1.45 @≤6 slots & dps>16, 1.22 @≤8 & dps>12) + dynamic overCap (2.4/1.9/1.6 by slotsRemaining) — was deferred "endgame scramble"
- [shipped] `computePosScarcity` counts supply fractionally across every eligible hard slot (multi-eligibility) — was deferred "posScarcity multi-elig"
- Monte Carlo re-verified: ~$3097 total spend (~99% of $3120), stranding small, stars ~1.0-1.2x — the endgame floors did NOT reintroduce overpricing

**Shipped (interactive — mock-interactive.js):**
- [shipped] step-wise AI bidding (one bump per beat; 550ms while user could re-engage, 150ms once user passed) — was deferred "staggered AI-bid pacing"
- [shipped] AI re-entry each step (removed permanent `passedTeams.add` for AI) so interactive price discovery matches the headless per-round re-evaluation — was deferred "unify interactive vs headless"
- [shipped] draft-day countdown timer (`_startMockTimer`/`_clearMockTimer`, default 12s, gen-guarded) with auto-pass on timeout — was deferred "countdown timer"
- [shipped] priced-out auto-pass (`_giveUserTheClock`): if max affordable <= current bid, auto-pass instead of prompting — polish-pass addition
- [shipped] bid log + lastSale captured on state for the ticker/banner

**Shipped (UI — mock.js):**
- [shipped] prominent SOLD banner (win/lose tint + your surplus) — was deferred "real SOLD banner"
- [shipped] live escalation ticker ("Matt $1 → … → Saxton $6", last bold) — was deferred "bid log"
- [shipped] countdown bar (shrinks, turns red ≤4s) in the bidding panel
- [shipped] Board search + "My needs only" filter + tier-cliff dashed dividers + per-position "need" badges — was deferred "board search/needs/tier cliffs"
- [shipped] Category Pace panel: pace-aware roto read, "Light on: …/Strong: …" — was deferred "live category projection"
- [shipped] focus preservation across re-renders in renderMock (custom-bid/search/nominate fields no longer lose keystrokes when the timer ticks) — was deferred "custom-bid focus"
- [shipped] keyboard: Enter = bid, P/Esc = pass; draft-day clock toggle on the start screen — polish

**Verification:** node -c all three; preview screenshots confirmed the bidding-phase UI (on-the-clock, verdict chip, countdown bar, ticker, bid controls, category pace, board filters) and that the full timer→auto-pass→sale→next-nominator loop runs autonomously end to end. Versions bumped to ?v=83; pushed to main (4c9c640).

**ALL Round 1 deferred items are now shipped.** Nothing deferred this round.

## Round 3 — 2026-07-02 — focus: everything EXCEPT Mock Draft (whole-product pass)

**Agents:** fantasy-baseball products expert (commercial-tool parity), UX designer (cross-tab IA/coherence), data-pipeline correctness engineer, maintenance/perf/accessibility engineer

**Shipped this round:**
- [shipped] Pinned supabase-js CDN to 2.110.0 (was floating @2) — maintenance
- [shipped] Tab persisted in location.hash; reload restores your view — UX
- [shipped] rAF-coalesced rerender() + guard so background loads don't wipe an in-progress Data-tab paste — maintenance/data
- [shipped] autoloadHostedRos/loadDraftDollars failures now surface (status warn) instead of silent .catch(()=>{}) — data/UX
- [shipped] fetch_ros_projections.py: ATC slug ratcdc only (plain full-season "atc" fallback REMOVED — was silently 2x-ing mid-season stats), + MIN_ROWS sanity gate — data (the ONE thing)
- [shipped] JSON-paste/file projection imports now set the manual-override flag — autoload no longer clobbers fresher hand-pastes — data
- [shipped] Data tab: per-source "Data as of" column (projection's own date, red past 7 days, manual tag) — a dead refresh job is now visible — data
- [shipped] Supabase: per-table errors counted into the status bar (was green "ok" on RLS failures); realtime subscribe status callback flags lost live sync — data
- [shipped] Standings auto-loads from ESPN on first view (parity with Keepers); what-if title-odds sims memoized per move (was 3000 sims per re-render); scroll preserved across re-renders — UX/perf
- [shipped] League App roster cache auto-refreshes past 12h (was pinned to localStorage all season) — data
- [shipped] Keeper salary/contract history lookups match by normalized name (accents/suffixes no longer fall into the $8 FAAB guess) — data/product
- [shipped] Compare microcopy: removed instructions for a ▶ button that doesn't exist — UX
- [shipped] Inflation badge labeled ("keeper infl" / "live infl") with explanatory tooltips — UX
- [shipped] --text-3 contrast bumped to ~5:1 — a11y
- [shipped] CLAUDE.md (architecture map, the two silent-failure conventions) + scripts/bump.sh (stamps ALL ?v= to commit count) + scripts/check-globals.sh (dup top-level name detector) — maintenance
- [shipped] Preview port moved to 8766 in .claude/launch.json (8765 is squatted by a respawning Ruby/WEBrick server)

**Deferred (with reason):**
- [shipped, same day] SECURITY: proxy worker now requires the UD_PROXY_KEY secret as x-ud-key on every request (401 otherwise); CORS pinned to ALLOWED_ORIGIN (= https://jwarshafsky.github.io) + localhost, replacing the any-.github.io check. Client sends the key from Settings (localStorage ud_proxy_key). Deployed + curl-verified. The worker's ANTHROPIC_API_KEY was found invalid (pre-existing); Jeff supplied a fresh key same day — installed via wrangler secret and verified with a live /claude round-trip.
- [deferred] Live Draft draft-day package: nominate typeahead w/ normalized matching (exact-miss currently corrupts inflation), max-bid chip + SOLD validation, AI assistant fed live auction state (on-the-clock/high-bid/opponent budgets), Supabase pick mirroring + CSV export — medium build, draft is ~8 months out
- [deferred] Hot FAs pitchers (currently hitters-only; biggest July-value gap) — needs windowed pitcher stats design
- [shipped 2026-07-03] Overview "Today" brief (UX's ONE thing): standings.js caches a snapshot (place, title odds, projection coverage, age) to localStorage `ud_standings_brief_v1` on each recompute; overview.js `renderDailyBrief()` shows it at the top in-season (Mar–Oct via `_isInSeason()`), below the draft cards off-season. Empty state → "Open Standings"; stale (>48h) → warn. (Top-pickup deferred — didn't want to force the FA-pool pull.)
- [shipped 2026-07-03] MiL keeper decision support: prospects showed blank Value. Jeff's call = auto, not manual — minor/callup rows now use the SAME projection lookup (`_keeperPredValue` → ROS/next-year projected $) as everyone else; when the source covers the prospect it shows their projected $ (full surplus, since MiL cost = $0) and ranks them among ML keepers, else a dim "no proj". (An earlier manual dynasty-$/ETA editor + `ud_prospect_values` store was built then removed at Jeff's request — do NOT reintroduce manual prospect entry.)
- [deferred] Trade tab: rename to clarify keeper-$ lens vs Standings' roto lens; replace prompt() with datalist; standings-context verdict
- [deferred] Season/Draft-Prep tab grouping; toasts instead of alert(); shared clickable player-name cell

**The ONE thing this round:** fix the hosted-projection trust chain (ratcdc slug + visible data-age) — the default auto-loaded numbers could be wrong AND stale with every indicator green.

**What this round chose NOT to cover:** Mock Draft (rounds 1–2), Roster/Scenarios/History tabs in depth, Supabase schema/RLS audit, the espn.js proxy protocol itself.

## Round 4 — 2026-07-04 — focus: Live Draft pipeline + Draft Mode + Setup lobby

**Agents:** data-pipeline/state engineer, fantasy-domain test-vs-real reviewer, frontend wiring QA, game-day failure-drill QA

**Shipped this round (all same day):**
- [shipped] Stale-feed gating: old captures no longer present as "● Live"; muted "Last capture … (not live)" + 🗑 Clear captured feed (app → ud-bridge clears chrome.storage, udFeedCleared marker stops draft-bridge resurrecting it) — pipeline (user-reported bug 1)
- [shipped] Same-league re-draft detection: INIT with far fewer picks than stored + stale feed rotates feed/event-log (fresh seen-map + fresh Supabase session) — pipeline (the real cause behind bug 1's "16 picks")
- [shipped] TEST MODE ISOLATION: mocks are generic (Team N from feed ids, $260, no keepers, FULL pool, no MiL exclusions, no real owners anywhere). Five-seam design: draftExcludedNames / _inflationKeeperSelections / computeLiveTeamStates / teamOpenSlotProfile / processEspnPicks branch on draftTestMode(); draftTeams()/getMyDraftTeam()/draftTeamLabel() helpers; My-team selector on Draft Setup (test only, ud_test_my_team) — domain (user-reported bug 2; also fixed mock picks landing on real owners' ledgers / team-11 picks vanishing)
- [shipped] Immediate SOLD/INIT flush + pagehide flush + nextSeq+=100 on restore (hidden-tab throttling, seq reuse), draftTabOpen widened 25s→75s implicitly via flush… (beat unchanged; watchdog now advises reloading BOTH tabs)
- [shipped] currentLotFromEvents: SOLD only clears its own lot; INIT is a lot boundary; quiet lots go IDLE ("⏸ Lot quiet Xm — paused") instead of blanking, 60-min hard cutoff — pipeline+drills
- [shipped] rerender() focus guard widened app-wide (was Data-tab only): background realtime/feed events can't eat half-typed lobby/strategy/search text — UI QA's ONE thing
- [shipped] Feed-panel buttons (mode/download/resync/clear/undo-suspects) wired ONCE via document delegation — the commissioner-undo button was dead outside the classic view
- [shipped] Esc: closes debrief overlay first, never fires from inputs (incl. activeElement check)
- [shipped] _ensureEspnNames no longer poisons the session on one failed fetch; placeholder "Player N" picks auto-repaired after a late name-map load
- [shipped] extPresent expires after 60s of bridge silence (extension reload detection)
- [shipped] cloud-sync reload guard also respects feed-driven lots (second device no longer hard-reloads mid-bidding)
- [shipped] is_mock updates the existing Supabase session row on mode flip; tombstones store deletion timestamps (legacy `true` could never resurrect a re-auction); rotowire estimate failure shows "unavailable" instead of eternal "estimating…"; loadDraftConfig confirms BEFORE applying (no half-applied cancel); ds-badj patches its row (no focus steal); switchView clears draft-mode class + manualView

**Deferred:** chrome.alarms-based heartbeat (background worker), automated one-writing-device enforcement, ESPN pause CLOCK-frame behavior (verify in next mock).

**The ONE thing this round:** test-mode isolation via the five-seam draft context — mocks were silently mis-attributing picks to real leaguemates and hiding 100+ draftable players.

**What this round chose NOT to cover:** Supabase schema/RLS audit, mock-draft engine (rounds 1–2), non-draft tabs.

## Round 5 — 2026-07-05 — Phase 2 Round 1 (perfection plan; findings REQUIRED failing repros)

**Agents:** state-machine prover, chaos engineer, domain auditor, UI contract tester (all Opus; repros in test/repro/)

**Shipped (every fix verified by its repro flipping to PASS):**
- [shipped] keeperCostFor() unifies keeper pricing (League contract cost ?? ESPN salary) across computeLiveTeamStates + all inflation/budget math — team strip and inflation badge had disagreed on the same screen (math-1; spec S-030/S-089 amended)
- [shipped] 0-slot teams have maxBid=0 (leftover cash is not a bid); recommendBid/ownerInterest no longer see phantom competitors (math-2, S-095)
- [shipped] pick list keyed to feed stream identity: same-league re-draft AND cross-league switches clear picks+tombstones (state-1 HIGH + chaos-2)
- [shipped] straggler BID after SOLD can't reopen a phantom lot; explicit re-nomination still can (state-2)
- [shipped] stale gate heals on tab closed→open: retained capture re-applied directly + resync ping (state-3)
- [shipped] layered pick persistence: quota → drop events backup → retry → emergency key; failures VISIBLE in status bar + diagnostics (chaos-1, S-164)
- [shipped] _onDraftEvents watermark = max(seq) (latent ordering hardening)
- [shipped] Test-mode lobby affordances gate on draftTestMode(): my-team selector + honest TEST status via the segment button too (UI F1, S-125)

**Field bugs from Jeff's Jul 4 mock (fixed same day, separate commits):** SSE/EventSource capture blackout (15-min gap), Supabase mirror dead since creation (missing GRANTs), accented nominations unrecognized (getPlayerValue normalized fallback).

**Verified clean by this round:** Supabase session-identity chain, clearFeed round trip, INIT dedup bursts, ext-reload seq bump, event-cap crossing, clock jumps, mode flips, all Round-4 UI fixes under adversarial bursts (input survival, Esc matrix, in-place updaters, view hygiene).

**The ONE thing this round:** the repro-required regime worked — 8/8 findings were real (zero hallucinated), and every fix is now a permanent regression gate.

**Not covered:** proxy/worker security depth, Rotowire parser drift, mock-draft engine. Round 2 of Phase 2 should rotate onto those + re-probe the new fixes.

## Round 6 — 2026-07-05 — Phase 2 Round 2 synthesis (5 agents; repro-required regime)

**Agents:** aged-state auditor, fix-interaction prover, static-consistency sweeper, proxy/external-data reviewer, dress-rehearsal agent (all Opus; repros in test/repro/p2r2-*)

**Shipped (every fix verified by its repro/scenario flipping to PASS):**
- [shipped] reconcileDraftContext(): Real mode's guarantees are LOAD-time — aged/synced league override cleared (a real draft could silently run as a generic mock: no keepers, Team N, $260) and mock-CONTEXT picks purged (keyed on espn:N picks, closing the home-league-mock blind spot) + the mock event log reset with them (aged-F1/F2, interact-F1, dress-F2). Placement lesson: end-of-file, an earlier call died in FEED_MODE_KEY's TDZ inside an empty catch.
- [shipped] rotation clears staleInfo/staleRetained/initState; _applyDraftFeed foreign-stream guard (skew-tolerant >60s so old bridge stamps can't drop picks); bridge feed adopts elog.startedAt = ONE stream identity (interact-F2 + simulator regression it caught)
- [shipped] draft-dollars: empty parse over non-empty map = garbage → keep cache + surface (Google serves 200-HTML error pages; silent $0 wipe of every budget adjustment)
- [shipped] is_mock is one-way per session (real→test flip can't relabel a finished real draft; test→real correction still allowed) (aged-F3)
- [shipped] nominations panel passes live draftedNames — was suggesting already-SOLD players all draft (dress-F1)
- [shipped] 10s tick refreshes Draft Mode's time-derived UI (pause banner via #dm-idle in-place patch, feed-age chips) (dress-F3)
- [shipped] invariants line escapes everything (last unescaped external surface); duplicate feed-clear-stale id split; bare renderInflationCurve/SpendingPace guarded; worker /espn/draft-capture TOKEN frames redacted; _fixPlaceholderNames patches Draft Mode in place; stale "call-ups" label removed
- [shipped, separate commits same day] seat auto-detection from JOIN URL param 3; SSE/EventSource capture fallback; PONG not stored

**Verified clean:** worker route table (all gated, error hygiene), Rotowire parser drift-tolerance + XSS boundary, all sync-key/state-shape/wiring inventories, 20+ mechanism-interaction pairs, full 40-lot dress rehearsal (money exact at every checkpoint, undo + mid-draft reload flawless, all Round-4/5 fixes holding).

**The ONE thing this round:** Real mode is now safe BY CONSTRUCTION against aged/synced/mock state — every guarantee moved from click-time to load-time.

**Not covered:** mock-draft engine (rounds 1–2), keeper-tab UI internals, post-draft audit tool (Phase 3 build). Stop-condition check: Rounds 5+6 both found material issues → at least one more round after the next mock.

## Round 7 — 2026-07-05 — focus: UD-native mock → Live Draft feed (new feature)

**Agents:** 6 dimension reviewers (pipeline-fidelity, timer/gen-guard, mode-cleanup/real-leak, cadence-trace math, UI-integration, test-quality) → each finding independently refuted by an adversarial verifier. 9 findings, 5 CONFIRMED, 4 REFUTED.

**Shipped this round (all 5 confirmed, fixed + regression-tested in test/ud-mock.test.js):**
- [shipped] CRITICAL — `startMockFeed` could wipe an in-progress REAL draft: the Practice start button renders in the Draft Mode top bar regardless of feed mode, and `_mfResetDraftState` cleared+persisted `_liveDraft.picks` with no guard. Fix: hide Start in Real mode (`renderMockFeedControls`), AND `confirm()` before clearing any non-`espn:` (real/manual) pick.
- [shipped] HIGH — mock poisoned `_espnIdToName` with synthetic 900001+ ids for the whole session; a same-session REAL draft (no reload) never fetched real names → every pick "Player <id>". Fix: `_espnNamesAreMock` flag (set via new `_seedMockEspnNames` in draft.js); `reconcileDraftContext` nulls the map on entering Real mode; `_ensureEspnNames` clears the flag on a real fetch.
- [shipped] HIGH (coverage) — the test's per-frame `await drain()` wasn't load-bearing (seeded names + `fetchEspnPlayers=null` meant `_applyDraftFeed` never really awaited). Added a real-async-path test: pick lands as "Player <id>" placeholder, later fetch sweeps it via `_fixPlaceholderNames`.
- [shipped] MEDIUM — the `#mf-skip-n` free-type input on the lobby lost focus/caret on every SOLD (full renderDraft rebuild). Fix: replaced with preset skip buttons (⏭ Lot / ⏭ 5/10/25/50 / ⏭⏭ To end) — no caret to steal.
- [shipped] LOW — skip/fast-forward fired while paused, emitting picks behind a "⏸ Paused" label. Fix: `_mfSkipControls` hidden while paused.

**Refuted (4):** verifier could not construct a real failure — no regression filed.

**Verified:** test.sh (22 mock tests incl. 8 new regressions), simulate-draft --seed 7 --all, check-globals all green; browser-confirmed (Real mode hides Start, preset skip buttons render, paused hides skip).

**Not covered:** the mock ENGINE's bidding realism vs owner history (rounds 1–2 territory), post-draft debrief/audit on a mock. Stop-condition: first review of this feature found 2 serious bugs → at least one more round warranted after Jeff's first real practice mock.

## Round 8 — 2026-07-05 — focus: practice-mock feature (rotated mix, post-fix re-audit)

**Agents:** 6 fresh reviewers (audit-the-Round-7-fixes, cloud/device-sync, state-machine/persistence, concurrency/perf, invariant-conservation, classic-view/consumers) → per-finding adversarial verify. 9 findings, **9 CONFIRMED, 0 REFUTED**.

**Shipped this round (all fixed + regression-tested):**
- [shipped] CRITICAL (×5 findings, one root cause) — the mock reuses the real draft's DEVICE-SYNCED keys (ud_live_draft_v1, ud_feed_mode, ud_league_override, ud_test_my_team), so its synthetic state mirrored to Cloudflare KV and could overwrite a REAL in-progress draft on another signed-in device (or flip it into mock/test context, bypassing the Real-mode reconcile). Fix: `_syncDraftLocal` gate in cloud-sync.js — these keys are device-local (skipped on BOTH push and pull) whenever `mockFeedActive()`/`draftTestMode()` OR the stored pick list is all espn:N. Real drafts still sync. New test/cloud-sync-gate.test.js (5 tests).
- [shipped] HIGH — manual↔feed dedup in processEspnPicks (espn.js) upgraded a manual pick's ESPN identity but not its `team`, so a mock-upgraded manual pick counted its price against two teams → I-MONEY error. Now re-keys `team` like the insert path.
- [shipped] HIGH/LOW — skip/fast-forward pumped ~230 frames synchronously, each triggering a full cockpit rebuild (render storm) + a transient invariants "SOLD with no pick" flash. Fix: `_mockFeed.pumping` flag (mockFeedPumping()) suppresses per-frame renders in updateDraftModeLive/_updateFeedActivityDom/processEspnPicks/_applyDraftFeed; `_mfFastForward` is now async — awaits the _applyDraftFeed pick-adds, then renders ONCE.
- [shipped] MEDIUM — leaving Test mode (setFeedMode 'off'/'real') didn't stop a running mock; now setFeedMode stops it.

**Verified:** test.sh (8 files: 24 mock + 5 cloud-sync + rest), simulate-draft --seed 7 --all, check-globals green; browser-confirmed skip-to-end (231/231, 0 err/warn, pumping cleared, single final render).

**Not covered:** the mock ENGINE's bidding realism (rounds 1–2). Stop-condition: Round 8 found a CRITICAL → at least one more clean round required before calling it done.

## Round 9 — 2026-07-05 — focus: practice-mock feature (rotated mix, R8-fix re-audit)

**Agents:** 6 fresh reviewers (audit-R8-fixes, escaping/XSS, reload/persistence, numeric edges, saved-configs/multi-entry, debrief/audit consumers) → per-finding adversarial verify. 6 findings, **4 CONFIRMED, 2 REFUTED**.

**Shipped this round (all fixed + regression-tested):**
- [shipped] HIGH — the R8 sync gate MISSED the reconcile PURGE write: switching from a finished mock into Real mode ran reconcileDraftContext → saveLiveDraft(EMPTY) to ud_live_draft_v1 AFTER leaving test mode (so ungated), pushing an empty pick list to Cloudflare KV and destroying the real draft cloud-wide. Fix (architectural): the mock now persists to a DEVICE-LOCAL key (ud_live_draft_mock_v1, not synced) via `_draftPersistKeys()`; the real key is never touched during a mock, and reconcileDraftContext now RELOADS the real draft from its own key instead of writing empty. Browser-verified: real draft preserved through a mock and restored on switch to Real.
- [shipped] HIGH — draft-log.js DRAFT_LOG.isMock was one-way (only set false) and not re-initialized per session, so a mock after a real draft in the same tab uploaded as is_mock=false (contaminating human tendency data). Fix: the new-session reset block sets `DRAFT_LOG.isMock = !!isMock`; within-session test→real correction preserved. New test/draft-log-mock.test.js.
- [shipped] LOW×2 — after a reload post-mock, the watchdog could false-alarm "stalled" off the dead mock's old event log once an unrelated ESPN tab opened (fixed: >20min silence is never a live stall), and the feed-activity line presented the finished mock's log as current (fixed: labels it "Last capture (ended)" when no live tab/connection).

**Verified:** test.sh (9 files: 26 mock + 5 sync + 3 draft-log + rest), simulate-draft --seed 7 --all (updated reloadApp for the mock key), check-globals green; browser-confirmed the real-draft preserve+restore across a mock.

**Refuted (2):** escaping/consumer concerns that verification could not turn into a real failure.

**Stop-condition:** Round 9 still found 2 HIGH data-integrity bugs (both extensions of the R8 sync class) → NOT clean yet; at least one more round.
